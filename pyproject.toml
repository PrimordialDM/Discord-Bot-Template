[project]
name = "discord-bot-master-reference"
version = "1.0.0"
description = "Production-scale discord.py master bot template and modular starter by PrimordialDM"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "PrimordialDM" }
]
license = { text = "MIT" }
keywords = ["discord", "discord-bot", "discord.py", "python", "slash-commands"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
pythonpath = ["."]
